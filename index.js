import { AppRegistry } from 'react-native';
import App from './App';

// Ce fichier est généra par défaut lors de l'initialisation du projet.
// On ne devrait jamais avoir à y toucher.
AppRegistry.registerComponent('CoursMiw', () => App);
