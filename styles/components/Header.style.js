import colors from '../colors.style';

const headerStyle = {
    backgroundColor: colors.backgroundSecond
};

const headerTitleStyle = {
    color: 'white'
};

export default {
    headerStyle,
    headerTitleStyle
};
