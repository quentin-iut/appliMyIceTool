import { StyleSheet } from 'react-native';
import colors from '../colors.style';

export default StyleSheet.create({
    container: {
        marginRight: 15
    },
    icon: {
        fontSize: 22,
        color: '#FFFFFF',
    }
});
